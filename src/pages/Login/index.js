import React from "react";
import { Container, View, Image, BackgroundImage } from "../../utils/styles";
import {colors} from '../../utils/colors';

import bkgLogin from '../../assets/images/background-login.jpg';
import imgLogin from '../../assets/images/pernalonga.jpg';

const Login = () => {
  return (
    <Container>
      <View width={'750px'} height={'500px'} radius={'20px'} color={colors.white}>
        
      </View>
    </Container>
  );
};

export default Login;
